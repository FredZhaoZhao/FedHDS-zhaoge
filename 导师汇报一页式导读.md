# 导师汇报一页式导读

## 论文题目

**Guarded Federated Unlearning via Retained-Set Curvature**

---

## 1. 这篇小论文主要在研究什么

本文研究的是：**在联邦训练已经完成之后，如何对某个客户端执行后处理式遗忘，同时尽量不破坏模型在其余客户端上的整体性能。**

问题背景是：

- 联邦学习训练后，某个客户端可能提出删除请求；
- 直接重训练最干净，但代价太高；
- 因此需要一种**不完全重训练的近似联邦遗忘方法**。

---

## 2. 本文关注的核心矛盾

联邦遗忘天然存在一个权衡：

1. 更新过弱，遗忘不明显；
2. 更新过强，整体模型性能会被破坏。

所以本文的重点不是单纯追求“遗忘越强越好”，而是研究：

> **能否在控制全局损失的前提下，提升遗忘效果。**

---

## 3. 本文方法的核心思想

本文建立在 **FedHDS** 联邦指令微调骨干之上，但贡献不在训练阶段，而在训练完成后的遗忘阶段。

核心思想是把两件事分开：

1. **forget-client gradient**  
   负责定义“要削弱谁的影响”；

2. **retained-set curvature**  
   负责定义“如何尽量不破坏其余客户端对应的整体效用”。

在此基础上，再加入一套 guard 机制，包括：

- global-loss guard
- norm clipping
- direction probing
- forget-loss guard

因此，本文提出的不是单一更新公式，而是一套**受控的后处理遗忘流程**。

---

## 4. 本文最关键的机制发现

实验说明：

- 直接使用 **forget-batch Hessian** 时，遗忘信号往往更强；
- 但这种方式更容易造成全局性能下降；
- 使用 **retained-set curvature** 时，整体更稳；
- 但它的**默认方向可能是错的**，甚至会导致遗忘失败。

因此，本文最关键的发现不是“retained-set curvature 天然更强”，而是：

> **retained-set curvature 是方向敏感的，只有经过方向验证和 guard 约束后，才能成为真正可控的联邦遗忘更新。**

---

## 5. 本文最终得到的结论

本文支持的是一个比较克制的结论：

> retained-set curvature 配合 guard 机制，可以形成一种更强调全局效用保持、更新过程可审计的近似联邦遗忘方案。

也就是说，本文的方法更适合被理解为：

- **utility-preserving**
- **auditable**
- **post-training approximate federated unlearning framework**

而不是一个在所有设置下都“最强”的遗忘算法。

---

## 6. 这篇小论文的主要贡献

可以概括为三点：

1. 提出将**遗忘信号**与**曲率参考**分离的后处理联邦遗忘思路；
2. 揭示 retained-set curvature 的**方向错位问题**；
3. 设计了一套结合方向验证和 guard 机制的**受控更新流程**。

---

## 7. 建议导师阅读时重点关注的部分

如果时间有限，最建议优先看以下内容：

1. **摘要 + 引言末尾贡献**
2. **第 3 节方法**
3. **第 5.2 节主对比实验**
4. **第 5.3 节方向与 guard 机制分析**
5. **结论**

其中，最核心的实验结论集中在：

- `5.2`：forget-batch 和 retained-set 的主权衡
- `5.3`：为什么 retained-set 默认方向会失败，以及 guard 为什么必要

---

## 8. 当前论文的定位

这篇工作不是“大而全”的联邦遗忘综述或系统论文，而是一篇**机制清楚、问题聚焦、结论克制的小论文**。

它的研究定位可以概括为：

> **在 FedHDS 训练后的大模型联邦微调场景下，探索一种基于 retained-set curvature 和 guard 机制的受控后处理联邦遗忘方法。**

