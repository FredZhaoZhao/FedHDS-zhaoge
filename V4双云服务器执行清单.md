# V4 双云服务器执行清单

## 1. 使用说明

这份清单的目标是把 [V4完善计划.md](<F:\FedHDS-zhaoge\V4完善计划.md>) 落成可执行命令。

但先说清楚两个**当前代码库级别的硬约束**：

1. **当前仓库没有 `Alpaca-cleaned` 的直接数据入口**
   - `main.py` 目前只支持：
     - `dolly`
     - `instruct`
   - 其中 `instruct` 走的是 `natural-instructions-2.8` 的目录结构，不是 `Alpaca-cleaned` 直接 JSONL
2. **当前仓库没有 `Fisher-diagonal forgetting` 和 `GAGR-lite` 的 CLI 入口**
   - 现有 `unlearn_method` 只有：
     - `second_order`
     - `ga`
     - `ga_guarded`

因此，这份清单分成两类：

- **A 阶段：现在就能跑的命令**
- **B 阶段：实现完成后再跑的命令模板**

如果你要求“今天就能开双机跑 A 阶段”，最稳的做法是：

- **用仓库原生支持的 `instruct` 路线代替 `Alpaca-cleaned`**
- 或者先单开一个小任务，给 `Alpaca-cleaned` 做数据接线

---

## 2. 约定变量

下面命令默认：

- **服务器 A**
  - 端口：`39636`
  - 地址：`connect.westc.seetacloud.com`
- **服务器 B**
  - 地址同上，端口请替换成你的第二台云服务器端口：`<PORT_B>`

远端仓库路径统一假设为：

```bash
/root/autodl-tmp/FedHDS-zhaoge-v3-dev
```

模型路径统一假设为：

```bash
/root/autodl-tmp/models/Qwen2-0.5B-ms
```

如果你的远端仓库名不是这个，先全局替换。

---

## 3. 本地准备命令

### 3.1 检查两台云机连通

```powershell
ssh -p 39636 root@connect.westc.seetacloud.com "hostname; nvidia-smi --query-gpu=name,memory.total --format=csv,noheader"
ssh -p <PORT_B> root@connect.westc.seetacloud.com "hostname; nvidia-smi --query-gpu=name,memory.total --format=csv,noheader"
```

### 3.2 检查远端仓库和脚本

```powershell
ssh -p 39636 root@connect.westc.seetacloud.com "cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev && ls scripts/run_v3_core_suite.sh scripts/summarize_experiments.py scripts/make_paper_table.py"
ssh -p <PORT_B> root@connect.westc.seetacloud.com "cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev && ls scripts/run_v3_core_suite.sh scripts/summarize_experiments.py scripts/make_paper_table.py"
```

### 3.3 同步本地最新代码到两台云机

如果你已经确认两台云机代码就是最新，可以跳过。

```powershell
scp -P 39636 scripts\run_v3_core_suite.sh root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/
scp -P 39636 scripts\summarize_experiments.py root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/
scp -P 39636 scripts\make_paper_table.py root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/
scp -P 39636 scripts\mia_loss_based.py root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/

scp -P <PORT_B> scripts\run_v3_core_suite.sh root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/
scp -P <PORT_B> scripts\summarize_experiments.py root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/
scp -P <PORT_B> scripts\make_paper_table.py root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/
scp -P <PORT_B> scripts\mia_loss_based.py root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/scripts/
```

---

## 4. A 阶段前置检查：第二数据集是否能直接开跑

## 4.1 当前仓库是否已具备 `instruct` 数据

这条命令检查远端是否已经有 `natural-instructions-2.8`：

```powershell
ssh -p 39636 root@connect.westc.seetacloud.com "test -d ~/.datasets/natural-instructions-2.8/tasks && echo READY || echo MISSING"
ssh -p <PORT_B> root@connect.westc.seetacloud.com "test -d ~/.datasets/natural-instructions-2.8/tasks && echo READY || echo MISSING"
```

如果输出 `READY`，说明**可以直接跑 `instruct` 路线**。  
如果输出 `MISSING`，说明你现在没有“零改代码”的第二数据集入口。

## 4.2 关于 `Alpaca-cleaned`

当前仓库**不能直接**用下面这种命令运行 `Alpaca-cleaned`：

```bash
DATASET=alpaca DATA=/path/to/alpaca.jsonl ...
```

因为代码里没有 `alpaca` loader。  
所以如果你坚持按计划用 `Alpaca-cleaned`，必须先完成：

1. `Alpaca-cleaned -> 仓库可读格式` 的数据接线
2. 对应 loader 或转换逻辑

在这件事没完成前，**A 阶段现在能直接跑的只有 `instruct` 路线**。

---

## 5. A 阶段：双机执行清单（现在可跑）

## 5.1 执行策略

为了不改现有脚本，A 阶段直接复用：

```bash
scripts/run_v3_core_suite.sh
```

这会带来一个现实后果：

- 它会**多跑一个 `ga`**
- 也会自动跑 `MIA + summary + figures`

这比计划中的 `6 methods` 略重，但换来：

- 不用现在改 runner
- 风险最低
- 两台云机今天就能起跑

论文里最后仍然可以只保留：

1. `fl`
2. `fedhds`
3. `retrain_oracle`
4. `forget_batch_hessian`
5. `retained_negative_guard`
6. `ga_guarded`

把多跑出来的 `ga` 作为附加对照或不用。

## 5.2 A1：Smoke Test（3 seeds）

### 服务器 A：跑 `seed42 seed43`

```bash
ssh -p 39636 root@connect.westc.seetacloud.com
cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev
tmux new -s v4_a1_A

export HF_HOME=/root/autodl-tmp/cache
export TRANSFORMERS_CACHE=/root/autodl-tmp/cache

ROOT=formal_cloud_results/v4_a1_instruct_seed5 \
ARCHIVE=v4_a1_instruct_seed5_A.tar.gz \
MODEL=/root/autodl-tmp/models/Qwen2-0.5B-ms \
DATASET=instruct \
SEEDS="42 43" \
IID=dir0.5 \
NUM_CLIENTS=20 \
ROUNDS=20 \
LOCAL_STEP=2 \
DATA_SAMPLE=0.4 \
FORGET_CLIENT_IDX=0 \
bash scripts/run_v3_core_suite.sh 2>&1 | tee formal_cloud_results/v4_a1_instruct_seed5_serverA.log
```

### 服务器 B：跑 `seed44`

```bash
ssh -p <PORT_B> root@connect.westc.seetacloud.com
cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev
tmux new -s v4_a1_B

export HF_HOME=/root/autodl-tmp/cache
export TRANSFORMERS_CACHE=/root/autodl-tmp/cache

ROOT=formal_cloud_results/v4_a1_instruct_seed5 \
ARCHIVE=v4_a1_instruct_seed5_B.tar.gz \
MODEL=/root/autodl-tmp/models/Qwen2-0.5B-ms \
DATASET=instruct \
SEEDS="44" \
IID=dir0.5 \
NUM_CLIENTS=20 \
ROUNDS=20 \
LOCAL_STEP=2 \
DATA_SAMPLE=0.4 \
FORGET_CLIENT_IDX=0 \
bash scripts/run_v3_core_suite.sh 2>&1 | tee formal_cloud_results/v4_a1_instruct_seed5_serverB.log
```

### A1 结束后的检查

服务器各自执行：

```bash
tail -n 40 formal_cloud_results/v4_a1_instruct_seed5_serverA.log
tail -n 40 formal_cloud_results/v4_a1_instruct_seed5_serverB.log
```

检查项：

- `fedhds/fl/retrain_oracle` 没有异常发散
- `retained_negative_guard` 正常出结果
- `summary.csv` 和 `paper_table.md` 生成

## 5.3 A2：正式版（补齐到 5 seeds）

如果 A1 健康，则补齐正式 `5 seeds`。

### 服务器 A：跑 `seed42 seed43 seed44`

```bash
ssh -p 39636 root@connect.westc.seetacloud.com
cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev
tmux new -s v4_a2_A

export HF_HOME=/root/autodl-tmp/cache
export TRANSFORMERS_CACHE=/root/autodl-tmp/cache

ROOT=formal_cloud_results/v4_a2_instruct_seed5 \
ARCHIVE=v4_a2_instruct_seed5_A.tar.gz \
MODEL=/root/autodl-tmp/models/Qwen2-0.5B-ms \
DATASET=instruct \
SEEDS="42 43 44" \
IID=dir0.5 \
NUM_CLIENTS=20 \
ROUNDS=20 \
LOCAL_STEP=2 \
DATA_SAMPLE=0.4 \
FORGET_CLIENT_IDX=0 \
bash scripts/run_v3_core_suite.sh 2>&1 | tee formal_cloud_results/v4_a2_instruct_seed5_serverA.log
```

### 服务器 B：跑 `seed45 seed47`

```bash
ssh -p <PORT_B> root@connect.westc.seetacloud.com
cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev
tmux new -s v4_a2_B

export HF_HOME=/root/autodl-tmp/cache
export TRANSFORMERS_CACHE=/root/autodl-tmp/cache

ROOT=formal_cloud_results/v4_a2_instruct_seed5 \
ARCHIVE=v4_a2_instruct_seed5_B.tar.gz \
MODEL=/root/autodl-tmp/models/Qwen2-0.5B-ms \
DATASET=instruct \
SEEDS="45 47" \
IID=dir0.5 \
NUM_CLIENTS=20 \
ROUNDS=20 \
LOCAL_STEP=2 \
DATA_SAMPLE=0.4 \
FORGET_CLIENT_IDX=0 \
bash scripts/run_v3_core_suite.sh 2>&1 | tee formal_cloud_results/v4_a2_instruct_seed5_serverB.log
```

### A2 fallback：如出现异常 seed，则用 `seed48`

只在有异常时补跑：

```bash
ssh -p <PORT_B> root@connect.westc.seetacloud.com
cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev
tmux new -s v4_a2_fallback

export HF_HOME=/root/autodl-tmp/cache
export TRANSFORMERS_CACHE=/root/autodl-tmp/cache

ROOT=formal_cloud_results/v4_a2_instruct_seed5 \
ARCHIVE=v4_a2_instruct_seed5_fallback.tar.gz \
MODEL=/root/autodl-tmp/models/Qwen2-0.5B-ms \
DATASET=instruct \
SEEDS="48" \
IID=dir0.5 \
NUM_CLIENTS=20 \
ROUNDS=20 \
LOCAL_STEP=2 \
DATA_SAMPLE=0.4 \
FORGET_CLIENT_IDX=0 \
bash scripts/run_v3_core_suite.sh 2>&1 | tee formal_cloud_results/v4_a2_instruct_seed5_fallback.log
```

---

## 6. A 阶段结果回传与本地合并

### 6.1 各服务器打包

服务器 A：

```bash
cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev
tar -czf v4_a2_instruct_seed5_serverA.tar.gz formal_cloud_results/v4_a2_instruct_seed5
```

服务器 B：

```bash
cd /root/autodl-tmp/FedHDS-zhaoge-v3-dev
tar -czf v4_a2_instruct_seed5_serverB.tar.gz formal_cloud_results/v4_a2_instruct_seed5
```

### 6.2 下载回本地

```powershell
scp -P 39636 root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/v4_a2_instruct_seed5_serverA.tar.gz F:\FedHDS-zhaoge\formal_cloud_results\
scp -P <PORT_B> root@connect.westc.seetacloud.com:/root/autodl-tmp/FedHDS-zhaoge-v3-dev/v4_a2_instruct_seed5_serverB.tar.gz F:\FedHDS-zhaoge\formal_cloud_results\
```

### 6.3 解压到分开目录

```powershell
New-Item -ItemType Directory -Force F:\FedHDS-zhaoge\formal_cloud_results\v4_a2_merge\serverA | Out-Null
New-Item -ItemType Directory -Force F:\FedHDS-zhaoge\formal_cloud_results\v4_a2_merge\serverB | Out-Null

tar -xzf F:\FedHDS-zhaoge\formal_cloud_results\v4_a2_instruct_seed5_serverA.tar.gz -C F:\FedHDS-zhaoge\formal_cloud_results\v4_a2_merge\serverA
tar -xzf F:\FedHDS-zhaoge\formal_cloud_results\v4_a2_instruct_seed5_serverB.tar.gz -C F:\FedHDS-zhaoge\formal_cloud_results\v4_a2_merge\serverB
```

### 6.4 合并时只拷 `seedXX/`

推荐合并目标目录：

```text
F:\FedHDS-zhaoge\formal_cloud_results\v4_a2_instruct_seed5
```

---

## 7. A.5 阶段：本地执行命令

这一阶段大部分不需要云 GPU。

### 7.1 先重生成总汇总

```powershell
cd F:\FedHDS-zhaoge
python scripts/summarize_experiments.py formal_cloud_results/v4_a2_instruct_seed5 --output formal_cloud_results/v4_a2_instruct_seed5/summary.csv
python scripts/make_paper_table.py formal_cloud_results/v4_a2_instruct_seed5/summary.csv --output-prefix formal_cloud_results/v4_a2_instruct_seed5/paper_table
python scripts/plot_experiments.py formal_cloud_results/v4_a2_instruct_seed5/summary.csv --output-dir formal_cloud_results/v4_a2_instruct_seed5/figures --formats png,pdf
```

### 7.2 需要人工/脚本补的表

这一步当前仓库里没有现成单独脚本，后续要补：

1. `per-seed accepted-steps` 表
2. `within-seed normalized` 表
3. `HP sensitivity` 重组表
4. `MIA sanity check` 结果汇总

建议新产物目录：

```text
formal_cloud_results/v4_a2_instruct_seed5/analysis_tables/
```

---

## 8. B 阶段：当前不能直接跑的部分

这里必须明确：

**当前仓库没有 `Fisher-diagonal forgetting` 和 `GAGR-lite` 的 CLI 入口，所以 B 阶段现在没有可直接执行的真实命令。**

在这两个 baseline 实现之前，不要尝试运行下面这种命令：

```bash
python main.py ... --unlearn_method fisher_diagonal
python main.py ... --unlearn_method gagr_lite
```

因为当前代码会直接报：

```text
Unsupported unlearning method
```

### 8.1 B 阶段实现完成后的命令模板

等实现完成后，B 阶段建议还是按双机平分 seeds：

#### 服务器 A

```bash
ROOT=formal_cloud_results/v4_b_external_seed5 \
MODEL=/root/autodl-tmp/models/Qwen2-0.5B-ms \
DATA=/root/autodl-tmp/FedHDS-zhaoge-v3-dev/data/databricks-dolly-15k.jsonl \
SEEDS="42 43 44" \
IID=dir0.5 \
... \
python main.py ... --unlearn_method fisher_diagonal
```

#### 服务器 B

```bash
ROOT=formal_cloud_results/v4_b_external_seed5 \
MODEL=/root/autodl-tmp/models/Qwen2-0.5B-ms \
DATA=/root/autodl-tmp/FedHDS-zhaoge-v3-dev/data/databricks-dolly-15k.jsonl \
SEEDS="45 47" \
IID=dir0.5 \
... \
python main.py ... --unlearn_method gagr_lite
```

但这部分要等 baseline 实现落地后，才能再给你一版真正可运行的终版命令。

---

## 9. 最推荐的实际执行顺序

### 今天就能做的

1. 确认两台云机都有 `natural-instructions-2.8`
2. 按上面的 `A1` 开双机 smoke test
3. A1 成功后直接跑 `A2`
4. 本地做 `A.5` 汇总

### 今天还不能直接做的

1. 用 `Alpaca-cleaned` 跑 A 阶段
2. 跑 `Fisher-diagonal forgetting`
3. 跑 `GAGR-lite`

这三件事在当前代码库里都还缺实现或接线。

---

## 10. 一句话总结

**按当前仓库状态，双云服务器现在能立刻开跑的是“第二数据集走 `instruct/natural-instructions` 路线的 A 阶段”；而 `Alpaca-cleaned` 数据接线和 `Fisher/GAGR-lite` 外部 baseline 仍属于下一步实现任务，不能假装已经有可执行命令。**
