# 第三次补强计划（v2.1，整合 codex 两轮反馈）

**论文题目**：Guarded Federated Unlearning via Retained-Set Curvature
**当前版本**：paper_draft_submission_v2_uestc_template.docx
**计划周期**：4 周（约一个月）
**计划起始日**：2026-05-18

**v2 修订**（第一轮 codex 反馈）：
- 组织方式从"周次驱动"改为"信号驱动 + 优先级队列"，避免单项卡住拖整体投稿
- 删除 FedEraser-lite、NPO、7B 模型扩展三项
- LiRA-lite 降级为 P4，loss-based MIA 单独前置为 P2
- α=0.1 非 IID 从主表降至 appendix
- 新增 GA + guards 作为关键 ablation baseline
- 新增低成本跨模型族验证（LLaMA-3.2-1B 或 Pythia-1.4B）
- 论文主线表述按 codex 建议重写（sign-misalignment + guarded controlled unlearning）
- Retrain baseline 重新定位为 oracle / upper-bound reference

**v2.1 修订**（第二轮 codex 反馈，针对仓库代码细节）：
- MIA 脚本路径从 `evaluations/mia_loss_based.py` 改为 `scripts/mia_loss_based.py`，避免与顶层 `evaluations.py` 冲突
- Non-IID 不需要改 `partition_data.py`，使用现有 `--iid dir0.5` 直接接线
- `accepted_steps` 等字段已在 `summary.csv` 中（`unlearn_steps_accepted` 等），P2 这部分是表格映射工作而非新实现
- P2 实验矩阵明确：4 unlearning methods × 5 seeds + 2~4 组 reference（retrain oracle 不进 critical path）
- P5 跨族首选 LLaMA-3.2-1B，Pythia-1.4B 需额外改 LoRA target modules，不是零成本平替
- 新增原则：MIA member/non-member 集与现有 zeroshot eval set 解耦
- 明确成功硬底线：D+28 前完成 P1+P2+P3，P4 只保 GA-with-guards，LiRA-lite 与 P5 不阻塞投稿

---

## 一、核心主线（论文最终要讲的故事）

按 codex 的建议，论文的中心论点统一为：

> 我们识别了 retained-curvature unlearning 中的 **sign-misalignment 问题**，并给出一种带 guard 的**可审计控制方案**，在 utility preservation 上优于更激进的 forgetting update，同时提供可测的 forgetting evidence。

不再使用以下两种写法：
- ~~"我们其实很强，只是之前没说好"（现有证据不支持）~~
- ~~"method is better understood as utility-preserving rather than universally strongest"（过度克制，自我削弱）~~

**Retrain baseline 重新定位**：定义为 oracle / exact-forgetting upper-bound reference，代表理想极限，**不属于本文要解决的 post-training regime**，因此不与之做胜负比较。

---

## 二、信号驱动优先级队列

按"对核心主线的支撑度"排序，从 P1 到 P5。前面项目未完成不开后面项目。

| 优先级 | 任务 | 目标产出 | 估时 |
|---|---|---|---|
| **P1** | Claims + MIA protocol 定义 | 一页技术备忘录 + 评估代码骨架 | 2 天 |
| **P2** | 主 setting 上 loss-based MIA + GA baseline + 5 seeds | 升级版 Table 3、新增 MIA AUC 列、accepted-steps 字段 | 7 天 |
| **P3** | Non-IID α=0.5 复现主对比 | 新增 §5.6，主对比在两种划分下的对照表 | 5 天 |
| **P4** | 第二 baseline（GA-with-guards 消融） + LiRA-lite | 主表新增 ablation 行 + 一组 LiRA AUC | 5 天 |
| **P5** | 跨模型族（LLaMA-3.2-1B 或 Pythia-1.4B） + 半页理论小节 | 一组跨族结果 + §3.9 supporting rationale | 5 天 |
| **W** | 全文重写、图表重做、相关工作扩充、投稿包 | paper v3 + cover letter + highlights + graphical abstract | 6 天 |

合计 30 天，与一个月缓冲对齐。

---

## 三、P1 — Claims 与 MIA Protocol 定义（2 天）

### 3.1 定义 claims 三元组

在动手做实验前，先把以下三个 claim 写死，所有后续实验只为验证这三条：

- **C1（识别）**：retained-set curvature update 存在 sign-misalignment 现象——默认方向在 utility guard 下可能让 forget loss 下降。
- **C2（修正）**：direction probing + forget-loss guard + stepped trust-region 三个机制叠加，可把 retained-set curvature 改造成可控的近似遗忘流程。
- **C3（定位）**：所得方法在 utility preservation 维度上优于直接 forget-batch Hessian update，同时通过 MIA 提供独立的 forgetting evidence；它不与 retrain oracle 比强度，而是在 post-training regime 内提供可审计的近似方案。

### 3.2 MIA Protocol 设计（最关键，不可糊弄）

按 codex 建议，先把 member / non-member 协议定义清楚，否则 AUC 不稳。

- **Member set**：forget client（client 0）的全部本地训练样本。
- **Non-member set**：与 forget client 类型分布对齐的 held-out 样本——从 Dolly 中划出一个 client 0 没见过的小集合，规模与 member 接近。
- **重要原则（codex 强调）**：现有 zeroshot 评估集来自 category 7 held-out（`utils_data/load_data.py:65`），它是 global utility 的评估集。**MIA 的 member/non-member 集必须单独定义，绝不复用 `eval_loader`**，否则攻击概念与 utility 评估混在一起，论文会被审稿人质疑攻击协议不干净。
- **攻击者知识**：仅访问模型 forward loss / logits，不访问梯度、不假设训练超参。
- **指标**：ROC-AUC + TPR@FPR=1%（后者比 AUC 对低 FPR 区更敏感，是当下 unlearning 评估的标配）。
- **基线检查**：在 pre-unlearning 模型上 attack AUC 必须显著高于 0.5，否则 MIA 信号本身就不强、后续无意义。如果在 IID 划分下 AUC 接近随机，立即切到 non-IID（与 P3 合流）。

### 3.3 P1 交付物

- [ ] `mia_protocol.md`：一页备忘录，写清 member/non-member 来源、攻击者知识、指标、判定标准。
- [ ] `scripts/mia_loss_based.py`（或扩展现有 `evaluations.py`）：骨架代码，能跑 FedHDS baseline 输出 AUC（不要求完整跑五种模型，只验证 protocol 跑通）。**注意**：仓库现有 `evaluations.py` 是顶层单文件（codex 已确认 `evaluations.py:1`），新代码若以 `evaluations/` 子包方式放置会与现有 import 命名冲突；推荐放在 `scripts/` 下，或者把 MIA 函数直接挂到 `evaluations.py` 末尾。
- [ ] 一份"如果 MIA 信号弱怎么办"的 fallback 决策表。

---

## 四、P2 — 核心 MIA + GA Baseline + 5 Seeds（7 天）

### 4.1 实验矩阵

在主 setting（Qwen2-0.5B、Dolly、20 clients、data_sample=0.4、rounds=20）下，跑以下模型并报告完整字段：

| 模型来源 | 必报字段 | seeds |
|---|---|---|
| FedHDS baseline（reference） | global loss、MIA AUC、MIA TPR@FPR=1% | 1~2 seeds（reference） |
| Retrain oracle（reference） | global loss、held-out forget client loss、MIA AUC（理论下界） | **1~2 seeds**（reference，不进 5-seed critical path） |
| **Gradient Ascent（纯版）** | global loss、forget loss Δ、update L2、MIA AUC、time | 5 seeds（42~46） |
| Forget-batch Hessian | 同上 | 5 seeds |
| Retained-set positive direction | 同上 + accepted_steps / total_steps | 5 seeds |
| Retained-set negative + guard（主方法） | 同上 + accepted_steps / total_steps | 5 seeds |

**关键约定**（codex 建议）：retrain oracle 是 reference，不与主方法做胜负比较，因此不进 5-seed critical path，1~2 seeds 足够。FedHDS baseline 同理。这样 critical-path 的 5-seed 实验只覆盖 **4 个 unlearning method × 5 seeds = 20 组 + 2~4 组 reference = 共 ~24 组**。

### 4.2 必须显式映射到主表的现有字段

codex 已确认（`scripts/summarize_experiments.py:7`）以下字段**已经在 summary.csv 中产出**，因此这部分**不是实现工作，而是"表格映射 + 论文写作"工作**：

| 论文表头 | summary.csv 字段 |
|---|---|
| accepted_steps / total_steps | `unlearn_steps_accepted` / `unlearn_num_steps`（或 `unlearn_steps_attempted`） |
| actual update L2 | `actual_param_delta_l2_norm` |
| guard stop reason | `unlearn_guard_stop_reason` |
| direction probe forget Δ | `unlearn_best_probe_direction_forget_delta` |

P2 在这块只需要做：
1. 改 `scripts/make_paper_table.py` 让上表字段显式进入论文表格。
2. 在论文 §4 实验设计里增加这些字段的语义定义段落。
3. 在 §5.2 主表加列，**不要再描述"新增字段"，那是已有的**。

Seed 44 那种 `unlearn_steps_accepted=0` 的情况单独留一行，不平均掉——这是方法可审计性的正面案例，对应 `unlearn_guard_stop_reason="forget_loss_guard"`。

### 4.3 Gradient Ascent 的"纯版"实现细节

- 仅在 forget client 的本地 batch 上做反向梯度上升。
- 与本文方法**保持相同的 step size（η=0.01）与 step 总预算**，使可比性最高。
- **不加** norm clip、step guard、direction probe，让 GA 充分暴露 utility cost——它的角色是"上限激进 forgetting"对照。

### 4.4 P2 交付物

- [ ] 主 critical-path：4 unlearning methods × 5 seeds = 20 组结果 + 2~4 组 reference（FedHDS baseline、retrain oracle）汇总 csv，合计 ~24 组。
- [ ] 升级版 Table 3：新增 MIA AUC 列、显式映射已有 accepted_steps / actual L2 字段，mean ± std 格式。
- [ ] 一张 scatter 图：x 轴 = global loss 增量，y 轴 = MIA AUC 下降量，每个方法一个点（含误差棒）。这张图是论文新主图。

---

## 五、P3 — Non-IID α=0.5 主对比复现（5 天）

### 5.1 范围

只做 **Dirichlet α=0.5**（moderate heterogeneity）。这是联邦学习公认的"非 IID 但不极端"的设定，足以证明方法不只在 IID 下成立。

α=0.1 不进主表。如果时间允许，做 1~2 行 sanity check 放 appendix；不允许就完全不做，写在 Limitations 里。

### 5.2 实验范围

在 α=0.5 下复现 P2 的 5 行主表 + Table 4 方向消融。无需再跑 5 个种子——只跑 seed 42，作为主 setting 的"非 IID 复现"对照即可。如果时间允许补 seed 43。

### 5.3 Non-IID 入口（已有支持，无需改造 partition_data.py）

codex 已确认（`utils_data/load_data.py:94`）：**仓库已经支持 Dirichlet 划分**，使用现有 CLI `--iid dir0.5` 即可，**不要新增 `--iid_split` 之类的新参数**（`main.py:37` 只有 `--iid`）。

因此 P3 的实际工作是**接线 + 复跑**，不是改造 `partition_data.py`：

- 在实验脚本里把 `--iid 0` 改成 `--iid dir0.5`，复用全部现有训练 + unlearning 链路。
- forget client 仍为 client 0，但因 Dirichlet 划分后 client 0 的样本分布与其他客户端不同，**这反而让 MIA 信号更强**——这是 codex 提到的 IID 下 MIA 信号弱的天然解药。
- 注意：`partition_idx_labeldir` 是按 `y_train` 标签 Dirichlet 划分（即按 task category 维度异构），不是按样本数量异构。这对 Dolly 这种 instruction-tuning 数据是合理的。

P3 因此从原来的"改造接口"任务降级为"实验跑通 + 写章节"，与 codex 评级 **高可行** 一致。

### 5.4 P3 交付物

- [ ] α=0.5 下 5 种模型 × seed 42 的对照表，作为 §5.6 主表。
- [ ] §5.6 新章节：「Non-IID Generalization」，约 1.5 页，包含一段 setup 描述 + 主表 + 一段机制讨论。

---

## 六、P4 — 第二 Baseline + LiRA-lite（5 天）

只在 P1~P3 完成的前提下做。

### 6.1 第二 Baseline：GA-with-Guards（ablation 风格）

这是我（claude）补充的设计、codex 没明确给的方案。

- 把 P2 的纯 GA 和本文主方法之间做一个 **ablation 对照**：GA 信号 + 本文的 norm clip + step guard + forget-loss guard，但**不用** retained-set curvature。
- 它回答的问题：**guard 机制本身贡献多少，curvature 信息贡献多少？**
- 如果 GA-with-guards 的 utility 表现接近本文主方法，说明 guard 是主因；如果显著差，说明 retained-set curvature 是主因。**两种结论都对论文有利**：前者证明 guard 是普适控制工具，后者证明曲率信息不可替代。

实现成本：在现有 server.py 加一个 `--unlearn_method ga_guarded` 开关，复用现有 guard 代码。约 2 天。

### 6.2 LiRA-lite（仅在 loss-based MIA 信号确认强后做）

- 训练 3 个 shadow model（圆数压到 10、data_sample 压到 0.2），仅在主方法和 forget-batch 两组上跑 LiRA。
- 不强求 5 seeds，单 seed 即可。
- 仅作为 P2 loss-based MIA 的 robustness check：若 LiRA AUC 与 loss-based AUC 的方向一致，论文 MIA 章节加一段"结论在更强攻击下保持"。
- 若超时或信号不一致，写进 future work，不阻塞投稿。

### 6.3 P4 交付物

- [ ] 主表新增 GA-with-guards 一行（5 seeds mean ± std）。
- [ ] §5.7 新章节：「Ablation: Curvature vs Guard Contribution」，约 1 页。
- [ ] （可选）LiRA-lite 结果一段，放 §5 末尾或 appendix。

---

## 七、P5 — 跨模型族 + 半页理论（5 天）

### 7.1 跨模型族验证（不是规模扩展，仍属低优先级）

明确放弃 7B 扩展（codex 论据成立：`stage4_large_model_data_20260504` 已显示大模型下 retained update 范数收缩、各变体趋同）。

改做**低成本跨族验证**：在 Qwen2-0.5B 配置基础上，**首选 LLaMA-3.2-1B** 跑主对比 + 方向消融，单 seed。

**关键警告（codex 指出）**：当前 LoRA target modules 在 `server.py:60` 写死为 `q_proj/k_proj/v_proj/o_proj/gate_proj/up_proj/down_proj`，这对 Qwen 和 LLaMA 系列天然匹配，但**对 Pythia 不直接适配**（Pythia 用 GPT-NeoX 架构，模块命名是 `query_key_value` 等）。因此：

- **LLaMA-3.2-1B 是真正的"零代码改"跨族验证**：模块命名匹配，直接换模型名即可。
- **Pythia-1.4B 不是平替**，是需要额外改 LoRA target modules 适配的潜在工作。如果 LLaMA 路径走通就不必动 Pythia；只有 LLaMA 显存或下载受阻时再考虑 Pythia，且明确接受会有 1~2 天的适配成本。

目的：证明方法不依赖 Qwen 系列特有的训练分布。LLaMA 路径成本 1~2 天显存可控。

P5 整体在 codex 评级中是 **低到中可行**，因此**不构成投稿阻塞项**。完成则进主文 §5.8，未完成则写进 future work。

### 7.2 半页理论小节（§3.9 supporting rationale）

按 codex 建议写成 supporting rationale，不写成完整收敛理论。具体提供：

1. **局部二次近似的合理性陈述**：在 θ* 附近用 Taylor 展开到二阶，说明 retained-set Hessian 是"在不破坏 retained-task 几何前提下走最陡方向"的局部最优近似。**用陈述句，不用定理。**
2. **Sign probing 的可靠性**：刻画为"一次 empirical risk check"，引用 trust-region 文献 [14] 说明这是常见做法。
3. **Stepped acceptance guard 的解释**：在 Lipschitz 平滑假设下，stepped guard 的接受准则可视为离散化的 trust-region radius adjustment。**给一个 Proposition 陈述，不给证明，引用 [14] 章节号。**

长度严格控制在半页内。**绝对不写定理-证明形式**。

### 7.3 P5 交付物

- [ ] 一组跨族模型主对比（约 6 行表）。
- [ ] §3.9 新小节，半页 supporting rationale。

---

## 八、写作重构（W 阶段，6 天）

P1~P5 不要等全部做完才动笔。**P2 结束就可以开始改写 Method 和 Results 章节**，让写作与实验并行。

### 8.1 必改章节清单

| 章节 | 改动要点 |
|---|---|
| Abstract | 按 §一 主线表述重写。强调三件事：识别 sign-misalignment、guard-based 控制方案、MIA-validated forgetting evidence |
| §1 Introduction | 删除自我削弱句，按主线表述改写贡献段。Retrain 作为 oracle 引入 |
| §2 Related Work | 扩到 35~50 篇，新增三段：LLM unlearning（TOFU、NPO、RMU、Eldan & Russinovich）、近期联邦 LLM unlearning、MIA-based unlearning evaluation |
| §2.3 Table 1 | 加一列「MIA evaluation provided」，把定性比较升级一格 |
| §3.9（新） | 半页 supporting rationale，参 §7.2 |
| §4 Experimental Design | 加 MIA protocol（引 P1 备忘录）、Dirichlet 划分描述、5 seeds 说明、accepted_steps 字段定义 |
| §5.2 Main Trade-off | **重写最关键**：加 MIA AUC 列；把 retrain "forget loss 不增" 改成 "retrain is the oracle; we report it for completeness, not as a baseline we aim to surpass"；加入 GA 一行 |
| §5.3 Direction Ablation | 加 mean ± std；Seed 44 全拒情况单独段落，定位为"guard 主动保护的正面证据" |
| §5.4 Scale | 删除/精简旧 2×2 scale 表；保留作为附录或简短段落 |
| §5.5 Seeds | 改为 5 seeds mean ± std + accepted-steps 分布讨论 |
| §5.6（新） | Non-IID α=0.5 主对比，参 §5 |
| §5.7（新） | Ablation: GA-with-guards vs 主方法，参 §6.1 |
| §5.8（可选） | 跨模型族验证，参 §7.1 |
| §6 Limitations | 缩短，主要保留"单数据集"与"二阶近似的浅近性"两条 |
| §7 Conclusion | 重写，按主线表述。**去掉 "bounded claim"**。 |

### 8.2 图表重做清单

- **Figure 1（新）**：方法 pipeline overview，包含 forget gradient → retained curvature HVP → direction probe → stepped guard 四步流程。
- **Figure 2**：FL vs FedHDS convergence（保留，重出 300 DPI）。
- **Figure 3（新主图）**：scatter 图，x 轴 global loss 增量、y 轴 MIA AUC 下降，每方法一个点 + 误差棒。**这张是新论文的封面级证据图。**
- **Figure 4（新）**：direction probing 示意图，两条候选轨迹 + guard 截断。
- **Figure 5（新）**：accepted_steps heatmap，5 seeds × 主方法各 setting。

所有图重出 300 DPI PNG + 矢量 PDF 双版本。

### 8.3 投稿包

- [ ] paper_draft_v3.docx（USTC 模板）+ 英文清稿版双备份
- [ ] Cover letter：开头三句话突出 P1~P3 三项实质性补强（MIA、GA baseline、non-IID）
- [ ] Highlights（KBS 必备）：5 条以内，对应核心 claim C1/C2/C3 + GA 对比 + MIA evidence
- [ ] Graphical abstract（KBS 必备）：用 Figure 1 简化版
- [ ] Response letter 草稿模板：预判 reviewer 会问的 5 个问题及答案（包括"为什么不和 FedEraser 比"、"为什么不做更大模型"）

---

## 九、参考时间表（仅供进度对齐，非硬约束）

按信号驱动优先级展开，每天数表示从 D+1 开始的天数：

| 日期 | 主任务 |
|---|---|
| D+1 ~ D+2 | P1：定 claims + MIA protocol + 备忘录 |
| D+3 ~ D+9 | P2：MIA 实现、GA baseline、5 seeds 主表跑完 |
| D+10 ~ D+14 | P3：Dirichlet 划分 + α=0.5 主对比 |
| D+15 ~ D+19 | P4：GA-with-guards ablation +（视情况）LiRA-lite |
| D+20 ~ D+24 | P5：跨模型族 + §3.9 理论小节 |
| D+8 ~ D+30 | W：写作重构（与实验并行，P2 结束即开始） |
| D+25 ~ D+28 | 图表重做 + 投稿包准备 |
| D+29 | 内审 1~2 人 |
| D+30 | 定稿提交 |

如果某优先级超时，**优先切割该优先级的非必要工作**（例如 P4 砍掉 LiRA-lite、P5 砍掉跨模型族），保留主线足够即可投稿，不延后整体节奏。

---

## 十、风险与应对预案

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| Pre-unlearning MIA AUC 接近随机 | 中 | 高 | 立即切到 non-IID（P3 上提到 P2 之前）；保留 IID 设置仅供对照 |
| Shadow model 训练超时 | 中 | 低 | 砍掉 LiRA-lite，只交付 loss-based MIA |
| Dirichlet 划分下 FedHDS 训练崩 | 低 | 高 | 把 α 调到 0.7~1.0，写明"轻度异构"；保留 IID 主表 |
| GA-with-guards 与主方法数值过于接近 | 中 | 中 | 写成"guard 是普适机制，curvature 提供方向先验"的双卖点叙事 |
| LLaMA 模型显存不够 | 中 | 低 | 改用 Pythia-1.4B 或干脆放弃跨族验证 |
| 内审后大改 | 中 | 高 | 预留 D+29 后 1~2 天缓冲；若仍不够，延后 3~5 天投稿 |
| 单项卡住超 3 天 | 中 | 中 | 立即跳到下一优先级，被卡项目按计划降级或砍掉 |

---

## 十一、投稿决策树

```
D+28 检查点：
├── P1+P2+P3 都完成？
│   ├── 是 + P4(GA-with-guards) 也完成 → 试投 KBS 或 Information Sciences
│   ├── 是 + P4/P5 部分完成 → 投 Knowledge-Based Systems
│   └── 否（P3 没完成）→ 投 Neurocomputing 或 FGCS
└── 连 P2 都没完成 → 推迟 2 周，绝不裸投
```

**首选**：Knowledge-Based Systems（IF ≈ 7.5，已引用过该刊同方向工作 [11]）
**备选**：Neurocomputing（IF ≈ 6，实验型友好）/ Future Generation Computer Systems（IF ≈ 7，系统叙事）
**进阶**：Information Sciences（仅在 P4 完整、MIA 结果干净时考虑）

---

## 十二、Checklist 总览

### P1（2 天）
- [ ] 写 claims 三元组 C1/C2/C3
- [ ] 写 mia_protocol.md
- [ ] 实现 mia_loss_based.py 骨架并在 FedHDS baseline 上跑通
- [ ] 验证 pre-unlearning MIA AUC > 0.5（否则触发 fallback）

### P2（7 天）
- [ ] 实现 Gradient Ascent baseline
- [ ] 4 unlearning methods × 5 seeds + 2~4 组 reference 主表跑完
- [ ] 修改 `scripts/make_paper_table.py`，把已有的 `unlearn_steps_accepted` / `unlearn_num_steps` / `actual_param_delta_l2_norm` 字段显式映射到论文表格列
- [ ] 升级版 Table 3（mean ± std）
- [ ] Figure 3 scatter 图（MIA AUC vs global loss Δ）

### P3（5 天）
- [ ] 使用现有 `--iid dir0.5` CLI 直接接线（无需改 `partition_data.py`）
- [ ] α=0.5 主对比 + 方向消融
- [ ] §5.6 Non-IID 章节初稿

### P4（5 天）
- [ ] GA-with-guards 实现
- [ ] §5.7 Ablation 章节初稿
- [ ] （可选）LiRA-lite 3 shadow models
- [ ] （可选）LiRA AUC 段落

### P5（5 天，非阻塞）
- [ ] **首选** LLaMA-3.2-1B 主对比（单 seed，LoRA target modules 无需改）
- [ ] Pythia-1.4B 仅作为 LLaMA 受阻时的备选，需先适配 `server.py:60` 的 target modules
- [ ] §3.9 supporting rationale 半页
- [ ] **若到 D+24 仍未完成，整段移入 future work，不阻塞投稿**

### W（贯穿后 3 周）
- [ ] Abstract / §1 Introduction / §7 Conclusion 按主线重写
- [ ] §2 Related Work 扩到 35~50 篇
- [ ] §5.2 Main Trade-off 重写（retrain 改 oracle，新增 GA，加 MIA 列）
- [ ] §5.3 Direction Ablation 加 mean ± std + Seed 44 段落
- [ ] §5.5 Seeds 改 5 种子均值方差 + accepted_steps 分布
- [ ] Figure 1 pipeline overview 新画
- [ ] Figure 4 direction probing 示意
- [ ] Figure 5 accepted_steps heatmap
- [ ] 所有图重出 300 DPI + PDF
- [ ] Cover letter
- [ ] Highlights × 5
- [ ] Graphical abstract
- [ ] 内审 1~2 人
- [ ] 定稿提交

---

## 十三、结语与最终成功标准

v2.1 计划在 v2 主线基础上做了 5 处技术校正（codex review 反馈）：

1. MIA 代码路径从 `evaluations/mia_loss_based.py` 改为 `scripts/mia_loss_based.py` 或扩展现有 `evaluations.py`，避免与顶层文件命名冲突。
2. Non-IID 入口使用现有 `--iid dir0.5`，不新增 `--iid_split` 参数；P3 从"接口改造"降级为"接线 + 复跑"。
3. `accepted_steps`、`actual update L2` 等字段**已在 `summary.csv` 中**，P2 这部分是**表格映射 + 写作**，不是新实现。
4. P2 实验矩阵明确为 4 unlearning methods × 5 seeds + 2~4 组 reference；retrain oracle 仅 1~2 seeds，不进 critical path。
5. P5 跨族验证首选 LLaMA-3.2-1B（LoRA target modules 自然匹配），Pythia-1.4B 需要额外适配，不是零成本平替。

新增一条原则（codex 强调）：**MIA member/non-member 集必须与现有 zeroshot eval set（category 7 held-out）解耦**，绝不复用 `eval_loader`，否则攻击协议会被审稿人挑出来。

### 最终成功标准（codex 建议，作为执行硬底线）

- **D+28 前必须完成 P1 + P2 + P3**。这是投稿底线。
- **P4 只保 GA-with-guards** 这一个 ablation；LiRA-lite 不许阻塞投稿。
- **P5（跨模型族 + 半页理论）任何一项都不许阻塞投稿**。完成则进正文，未完成则进 future work。
- 单项卡住超 3 天立刻按优先级降级，跳到下一项。

### codex 评级对应的执行节奏

| 评级 | 项目 | 处理方式 |
|---|---|---|
| 高可行 | P1、P3、W 重写 | 按时间表执行，作为投稿底层 |
| 中可行 | P2（瓶颈）、P4 GA-with-guards | 重点保障 P2，P4 简单且回报高，必做 |
| 低到中可行 | LiRA-lite、P5 跨族、P5 理论 | 全部非阻塞，按余力做；时间不够直接砍 |

### 一句话总结

一个月内按 P1→P2→P3 完成即可投 Neurocomputing；推进到 P4 即可投 KBS；P5 完整可挑战 Information Sciences。**投稿决策按 D+28 实际进度执行，绝不为等高优先级而延后**。

