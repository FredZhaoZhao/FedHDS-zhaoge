# V4 完善计划

## 1. 目标

当前 `V3` 版本已经具备正式期刊投稿基础，但若希望更稳地冲击 `KBS`，需要在不推翻主结论的前提下，做一轮 **V4 定向补强**。

本计划采用 **先 A，后 B** 的阶段化路线，并在实验之外明确加入写作和投稿准备阶段：

- **A**：跨数据集主验证
- **A.5**：用现有 `V3` 数据做免费升级，并把 `MIA` 补成规范化附录
- **B**：外部 baseline 补强
- **C**：论文重写与 `reframe`（重构主叙事）
- **D**：`KBS` 投稿准备

这套路线的核心原则：

1. 不重跑整套 `V3`
2. 不把 `MIA` 当主战场
3. 优先补 `KBS` reviewer 最容易攻击的两个点：
   - 单数据集
   - 缺外部 baseline
4. 同时把当前 `V3` 已经有、但论文里还没充分写出来的强论据抬到台面上

---

## 2. 当前 V3 的真实问题：以 `summary.csv` 为准

只看论文正文和直接看 [summary.csv](<F:\FedHDS-zhaoge\formal_cloud_results\v3_dir05_seed5\summary.csv>)，结论不是一回事。`V4` 必须正视下面这些现有硬问题。

### 2.1 Baseline 跨 seed 不稳定

当前 `fedhds final_global_loss` 在 5 个有效 seed 上分别约为：

- `seed42`: `1.5507`
- `seed43`: `0.1255`
- `seed44`: `2.0226`
- `seed45`: `0.0840`
- `seed47`: `3.0237`

这意味着同一配置下，baseline final loss 跨度从 `0.0840` 到 `3.0237`，量级差异很大。

这件事不能继续只靠正文里一小段 “within-seed FedHDS baseline 作为锚点” 来轻描淡写地处理。`V4` 必须：

1. 主动承认 baseline 跨 seed 波动很大
2. 强调所有 unlearning 比较都以 **within-seed FedHDS checkpoint** 为锚
3. 增加 **within-seed normalized** 视角，例如：
   - `Δglobal`
   - `Δglobal / fedhds_baseline`

### 2.2 retained_negative_guard 最强的卖点是 `Δglobal` 小方差

当前 5 个有效 seed 上，`retained_negative_guard` 相对同 seed `fedhds` 的 `Δglobal` 约为：

- `+0.0551`
- `+0.0445`
- `+0.0715`
- `+0.0650`
- `+0.0731`

均值确实是论文里写的 `+0.0618`，但更重要的是：

**它的方差很小，5 个 seed 都落在 `+0.04 ~ +0.07` 区间内。**

这比单独写 mean 更能说明方法的稳定性，是 `V4` 的免费升级卖点之一。

### 2.3 forget_batch_hessian 的不稳定程度要重新 framing

当前 5 个有效 seed 上，`forget_batch_hessian` 的 `Δforget` 跨度从：

- `-0.01`

到：

- `+14.43`

同时还伴随：

- `seed42 final_global_loss = 18.5533`

这比“4/5 positive forgetting”更重要。更准确的写法应该是：

**forget-batch curvature 在当前 LLM 联邦遗忘 setting 下是不可靠的更新方向；所谓“成功 seed”很多只是恰好朝着爆炸但忘对的方向走了。**

### 2.4 当前 `MIA` 对所有方法都几乎失效

现有 5-seed mean `MIA AUC` 大致都在 `0.49 ~ 0.50` 附近，包括：

- `fedhds`
- `fl`
- `forget_batch_hessian`
- `retained_negative_guard`
- `retrain_oracle`

这说明当前 `loss-based MIA` 在这个 regime（`LoRA + 小样本 + 当前协议`）下几乎是随机猜测。

这不是某个方法的锅，而是：

**这个 `MIA` 评估器在当前 setup 下整体信号弱。**

因此 `V4` 必须：

1. 在正文中给出机制性解释
2. 做一个 `MIA sanity check`
3. 明确把 `MIA` 降格为 supplementary indicator

### 2.5 accepted steps 也存在显著 seed 差异

`retained_negative_guard` 的 `accepted / attempted` 大致为：

- `seed42`: `3/4`
- `seed43`: `4/5`
- `seed44`: `1/2`
- `seed45`: `5/5`
- `seed47`: `3/4`

其中 `seed44` 只接受了 `1/2` 步。

这不能只在文中写一句平均 `3.2/4.0` 就带过去。`V4` 必须：

1. 补一张 `per-seed accepted-steps` 表
2. 主动解释 `seed44` 为什么 guard 触发得更早
3. 把“早停是 guard 工作正常的证据”写清楚

---

## 3. V4 的最终目标

`V4` 不是为了把论文变成“顶刊压制型大稿”，而是为了把当前稿件提升到更稳冲 `KBS` 的水平。

具体来说，`V4` 希望做到：

1. 不是只在 `Dolly` 上成立
2. 不是只在“自家对照体系”里成立
3. 把现有数据中最强的稳定性论据充分写出来
4. 把现有弱点主动解释，而不是等 reviewer 来打

---

## 4. 总体阶段划分

### A：跨数据集主验证

目标：

- 在第二数据集上复现当前主叙事
- 保持与 `V3` 主实验在 seeds 和方法上的对称性

### A.5：免费升级与附录整理

目标：

- 用现有 `V3` 数据补齐 reviewer 最可能问到的表和解释
- 把 `MIA` 做成规范化补充，而不是主证据

### B：外部 baseline 补强

目标：

- 至少加入两个实现成本可控、文献可引用的外部 baseline
- 覆盖一阶和二阶两类外部方法

### C：论文修改与主线重构

目标：

- 把 `sign sensitivity`（方向敏感性）提升为首要贡献
- 把 `forget_batch_hessian` 的极端波动与 `retained_negative_guard` 的低方差 utility 成本写成主卖点

### D：KBS 投稿准备

目标：

- 完成 `KBS` 所需格式、材料和 cover letter

---

## 5. 阶段 A：跨数据集主验证

## 5.1 数据集

优先选择：

1. `Alpaca-cleaned`
2. `Self-Instruct` 子集

推荐首选 `Alpaca-cleaned`，因为与 `Dolly` 同属 instruction tuning（指令微调）风格，迁移成本最低。

## 5.2 最终实验矩阵

模型与配置尽量与 `V3` 保持一致：

- 模型：`Qwen2-0.5B`
- 客户端数：`20`
- forget client：`client 0`
- `Dirichlet alpha=0.5`
- `rounds=20`
- `local_step=2`

方法数改为 **6 个**，保留 `fl`，不要砍：

1. `fl`
2. `fedhds`
3. `retrain_oracle`
4. `forget_batch_hessian`
5. `retained_negative_guard`
6. `ga_guarded`

seed 配置改为：

- 正式 seeds：`42, 43, 44, 45, 47`
- 备用 seed：`48`

说明：

- `seed46` 已在 `V3` 中确认是异常训练 seed，不再进入主结果
- 若 Alpaca 上出现新的异常 seed，可用 `seed48` 做替补

## 5.3 内部执行建议

为了避免一次性全开导致排障困难，可以分成两步：

### A1：内部 smoke test

- `3 seeds`
- 先快速确认：
  - 数据接线
  - 训练能跑通
  - 指标量纲正常

### A2：正式入论文结果

- 补齐到 `5 seeds x 6 methods`
- 只有这一步的结果才作为 `V4` 正式证据

也就是说：

- `3 seeds` 可以做内部判断
- **最终写进 KBS 版论文的必须是 5 seeds**

## 5.4 阶段 A 成功判据

这里给的是 **内部推进阈值**，不是论文中的理论阈值。

满足以下 4 条中的至少 3 条，可判定 A 阶段健康：

1. `retained_negative_guard` 的 mean `Δforget ≥ +0.10`
2. `retained_negative_guard` 的 mean `Δglobal vs fedhds ≤ +0.15`
3. `retained_negative_guard` 的 positive forgetting seeds `≥ 4/5`
4. `forget_batch_hessian` 至少出现一个明显不稳定 seed，例如：
   - `Δglobal > +1.0`
   - 或 forgetting / utility 明显失衡

## 5.5 阶段 A 的第三种结论：混合结果

阶段 A 不能只分成“成功 / 失败”两类。

最可能出现的情况是：

- 跨数据集主叙事仍成立
- 但效应量变小、波动变大，或者某些 seed 上不再那么整齐

这时的处理方式不是简单判失败，而是：

- 将第二数据集结果写成 **supplementary cross-dataset evidence**
- 在正文中主动说明跨数据集差异
- 仍保留 `KBS` 路线，但 cover letter 中要提前解释

## 5.6 阶段 A 的预计耗时

按正式版 `5 seeds x 6 methods` 估算：

- **单机：`18 ~ 24 小时`**
- **双机：`10 ~ 14 小时`**

原先的 `9 ~ 12 小时` 估计偏乐观，不再使用。

---

## 6. 阶段 A.5：免费升级 + MIA 规范化

这一阶段尽量复用现有数据，不依赖大规模新实验。

## 6.1 免费升级 1：写出 `Δglobal` 小方差

把 `retained_negative_guard` 的 `Δglobal` 稳定区间写成正文卖点：

- 5 seeds 全部落在 `+0.04 ~ +0.07`

不要只写 mean。

## 6.2 免费升级 2：重写 forget_batch_hessian 的风险叙事

在正文中明确写出：

- `Δforget` 从 `-0.01` 到 `+14.43`
- `seed42 final_global_loss = 18.5533`

将其定位为：

**不可靠更新方向**，而不是简单的“有时不稳定”。

## 6.3 免费升级 3：补 per-seed accepted-steps 表

增加一张表，列出：

- 每个 seed 的 `accepted / attempted`
- `guard stop reason`

并对 `seed44 = 1/2` 做解释。

## 6.4 免费升级 4：MIA 机制解释

在 `MIA` 小节主动写清楚：

- 当前 `loss-based MIA` 在 `LoRA + 小样本 + 当前协议` 下整体信号弱
- 这不是某个方法单独的失败
- 因而 `MIA` 只能作为 supplementary indicator

## 6.5 免费升级 5：把 sign sensitivity 提到首要贡献

`Introduction` 和 `Abstract` 中，把：

- `retained-set curvature` 的 `sign sensitivity`

从“中间机制发现”提升为全文首要贡献之一。

## 6.6 MIA sanity check

必须增加一个低成本 sanity check，用来证明 `MIA pipeline` 本身不是坏的。

候选方案：

1. baseline 模型 vs 随机模型
2. `fedhds` vs 一个故意过拟合 forget client 的模型

目标不是做漂亮数字，而是给 reviewer 一个交代：

**MIA 工具链本身可工作，只是在当前主要实验 regime 里信号弱。**

## 6.7 HP sensitivity（超参数敏感性）重组

利用现有 stage3 / norm sweep 数据，重组出：

- `per-seed × per-norm` sensitivity 表

不要求新跑，只要求重新汇总。

## 6.8 within-seed normalized 视角

在 `V4` 中必须增加：

- `Δglobal`
- `Δglobal / fedhds_baseline`

这可以把 baseline 跨 seed 的大幅波动，从潜在弱点转成显式的方法学说明。

## 6.9 阶段 A.5 耗时

预计：

- **双机墙钟：`4 ~ 6 小时`**

这部分很多是整理和出表，不是 GPU 纯训练时间。

---

## 7. 阶段 B：外部 baseline 补强

## 7.1 彻底移除的路线

`FedEraser-lite` 不再作为 `V4` 计划的一部分。

原因：

- 需要历史 client updates
- 需要改训练管线
- 和当前 `post-training from fixed checkpoint` 主线不共形
- 真实成本远高于原计划估算

## 7.2 新的 baseline 选择

阶段 B 改为两个外部 baseline：

1. **Fisher-diagonal forgetting**
   - 二阶风格
   - 实现成本可控
   - 与当前 curvature 主线相容

2. **GAGR-lite**
   - `gradient ascent + gradient restriction`
   - 一阶风格
   - 作为外部一阶对照

这个组合优于单个 `FedEraser-lite`，因为它覆盖了：

- 外部二阶家族
- 外部一阶家族

## 7.3 推荐实验矩阵

优先在 `Dolly` 主设置上跑：

- 模型：`Qwen2-0.5B`
- `Dirichlet alpha=0.5`
- seeds：`42, 43, 44, 45, 47`

如果 `Dolly` 上结果健康，再考虑是否在 Alpaca 上补 `3 seeds`。

## 7.4 阶段 B 成功判据

阶段 B 的最低验收标准：

**外部 baseline 必须能被清晰地放在 `forget_batch_hessian` 与 `retained_negative_guard` 构成的 forgetting-utility 光谱上。**

具体来说：

1. 结果不能完全无法解释
2. 结果不能把现有主叙事打成混乱状态
3. 如果某个外部 baseline 双指标都显著强于 `retained_negative_guard`，则不能硬写成“我们依然最好”，而要调整 framing

## 7.5 阶段 B 耗时

按 `Dolly 5 seeds + 2 baselines` 估算：

- **单机：`12 ~ 16 小时`**
- **双机：`8 ~ 12 小时`**

---

## 8. 阶段 C：论文修改与 reframe

这一阶段不能省略。

## 8.1 需要修改的核心逻辑

1. 把 `sign sensitivity` 提到首要贡献
2. 把 `forget_batch_hessian` 的极端波动写成关键反证
3. 把 `retained_negative_guard` 的低方差 utility 成本写成关键卖点
4. 把 `accepted-steps` 的 seed 差异和 `seed44` 早停写成机制证据
5. 在 `MIA` 小节加入机制性解释

## 8.2 需要修改的章节

至少包括：

- `Abstract`
- `Introduction`
- `Experimental Design`
- `Results`
- `Limitations`
- `Conclusion`
- `Appendix`

## 8.3 需要新增的内容

1. 第二数据集结果小节
2. 外部 baseline 小节
3. per-seed accepted-steps 表
4. within-seed normalized 表/图
5. `MIA sanity check` 描述
6. `HP sensitivity` 小节

## 8.4 阶段 C 耗时

预计：

- **`2 ~ 3 天`**

---

## 9. 阶段 D：KBS 投稿准备

这一阶段专门面向 `KBS`。

## 9.1 必备材料

1. `Highlights`
2. `Cover Letter`
3. `CRediT author statement`
4. `Conflict of interest statement`
5. Elsevier 模板下的最终排版文件

## 9.2 可选但建议准备

1. `Graphical Abstract`
2. 推荐审稿人名单
3. reject 后转投 `Neurocomputing` 的备用版本

## 9.3 Cover letter 的 framing

要主动写清楚：

- 这不是又一个“最猛遗忘算法”
- 本文的核心是：
  - 揭示 retained-set curvature 的 `sign sensitivity`
  - 给出 direction probing + guard control 的可控近似遗忘框架
- 当前联邦遗忘文献中，像这样处理异常 seed、5-seed robustness、跨数据集验证和 utility trade-off 的证据链并不多

## 9.4 阶段 D 耗时

预计：

- **`0.5 ~ 1 天`**

---

## 10. 双云服务器执行建议

## 10.1 是否值得两台并跑

值得。

双机的主要收益不是减少总 GPU 工时，而是：

1. 缩短墙钟时间
2. 降低单机中断带来的排期风险
3. 让 `A` 和 `B` 有机会部分重叠

## 10.2 推荐分工

### 服务器 A

- 主跑阶段 A：`Alpaca-cleaned 5 seeds x 6 methods`

### 服务器 B

- 在 A 中后期开始：
  - 整理 `A.5`
  - 预跑 `B` 阶段外部 baselines 在 `Dolly` 上的实验

这样做的好处是：

- `A` 结束时，`B` 不需要从零开始
- 总墙钟时间能再压缩

---

## 11. 最终时间预算

### 实验部分

- **A**：双机 `10 ~ 14 小时`
- **A.5**：双机 `4 ~ 6 小时`
- **B**：双机 `8 ~ 12 小时`

实验总预算：

- **双机 `22 ~ 32 小时`**
- 约 **1.5 天连续墙钟**

### 写作与投稿部分

- **C**：`2 ~ 3 天`
- **D**：`0.5 ~ 1 天`

### 总墙钟预算

- **`5 ~ 7 天`**

---

## 12. 版本管理建议

推荐按下面节奏推进：

1. `V4-lite`
   - 完成阶段 `A + A.5`

2. `V4`
   - 完成阶段 `B`

3. `V4-KBS-submit`
   - 完成阶段 `C + D`

推荐文件命名：

- `paper_draft_submission_v4.md`
- `paper_draft_submission_v4_uestc_template.docx`
- `v4_experiment_report.md`
- `kbs_submission_package/`

---

## 13. 最终执行摘要

如果用一句话概括这份修订后的计划：

**先用 Alpaca-cleaned 做与 Dolly 对称的 `5 seeds x 6 methods` 跨数据集主验证，再用 `Fisher-diagonal forgetting + GAGR-lite` 补齐外部 baseline，同时把现有 V3 数据中的小方差 utility、forget-batch 极端波动、accepted-steps 差异和 MIA 弱信号机制解释全部显式写进 V4；最后预留独立的论文重写与 KBS 投稿准备阶段。**
